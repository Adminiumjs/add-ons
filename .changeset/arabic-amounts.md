---
'@adminium/add-on-invoices': patch
---

On an Arabic printed copy an amount reads as it is written, its currency beside its digits, instead of being turned around by the page's right-to-left direction.
