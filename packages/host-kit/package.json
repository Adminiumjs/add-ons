{
  "name": "@adminium/add-on-host-kit",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "The retrofit kit: the AddOnHost seam and its guards, as one installable thing, so a twelfth host is a supported operation rather than a hand-copy.",
  "license": "AGPL-3.0-only",
  "exports": {
    ".": "./src/index.ts",
    "./guards": "./src/guards/index.ts"
  },
  "scripts": {
    "build": "echo 'packages/host-kit publishes TypeScript sources; there is nothing to build' >&2",
    "typecheck": "tsc -b",
    "test": "vitest run",
    "test:watch": "vitest",
    "install-into-host": "bash scripts/install-host-kit.sh"
  },
  "devDependencies": {
    "@adminium/add-on-host": "*",
    "jsdom": "^29.1.1"
  },
  "peerDependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "typescript": "^5.7.2"
  }
}
